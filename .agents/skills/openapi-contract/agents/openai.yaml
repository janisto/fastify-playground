interface:
  display_name: "OpenAPI Contract"
  short_description: "Maintain and verify the generated API contract"
  default_prompt: "Use $openapi-contract to update and verify OpenAPI, schema discovery, and runtime API contracts for the requested change."
